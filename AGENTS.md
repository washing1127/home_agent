# AGENTS.md

## 项目目标

本项目是仅服务一个自有家庭的家庭健康助理。产品以移动端 PWA 聊天为入口，使用 TypeScript 自主实现 Agent Core 和插件体系，并通过可替换的云 API 获取 LLM、ASR、TTS、图片、PDF、营养、天气和地点能力。

开始工作前先阅读 `docs/需求/家庭健康助理-v1-产品需求.md`。需求变更应先更新需求文档，再调整设计和实现。

## 目录与文档

- `docs/需求/`：产品需求和范围。
- `docs/设计/`：架构、数据、接口和交互设计。
- `docs/计划/`：实施与迭代计划。
- `docs/决策/`：需要长期保留的技术决策。

普通文档使用中文文件名，格式为 `家庭健康助理-v{版本}-{文档类型}.md`。决策记录使用 `YYYY-MM-DD-{中文主题}.md`。文档开头统一注明文档状态、日期和适用范围；同一事实只保留一个权威来源，避免复制后产生冲突。

## 核心约束

- 始终区分实际操作者与健康事件主体，禁止依赖模型判断登录身份。
- 原始对话和附件永久保存；提取出的记忆可修改、撤回并追溯来源。
- 普通成员可询问或代为记录其他家庭成员的信息；管理员可查看全部数据。
- v1 的家庭公共知识只在用户明确要求时写入，自动提取默认关闭。
- 健康业务数据由本系统管理，不存入供应商或通用 Agent 框架的专有记忆。
- 外部能力通过版本化 Provider 或工具接口接入；插件不得绕过业务服务直接修改核心数据。
- v1 不运行本地模型；外部 API 失败不能导致消息、附件或已确认数据丢失。
- 相关性不得表述为确定因果，未经确认的 AI 推测不得写成正式健康事实。

## 开发原则

- 优先模块化单体，不为假设中的规模提前拆分微服务。
- TypeScript 使用严格类型；HTTP、模型、工具、持久化和队列边界必须做运行时校验。
- 新功能优先通过聊天和通用健康事件实现，确认高频后再建设专门页面或模块。
- 变更身份、记忆、提醒或附件流程时，必须添加覆盖失败、重试、幂等和跨成员隔离的测试。
- 不提交 API 密钥、家庭健康数据、生产附件或包含敏感正文的调试日志。
- 未经用户明确许可，不连接、修改或部署到远程服务器。
